//Arrow Function with IMPLICIT RETURN

//If the function body has only a single expression, you can write it in a more concise form.
//The function body is replaced with the expression, and the return statement is implicit.

const multiply = (a, b) => a * b;

console.log(multiply(4, 3));
