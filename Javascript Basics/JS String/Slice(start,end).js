//extracts a part of the string and returns it as a new string. The end parameter is optional and non-inclusive.


let str = "Hello, World!";
let slicedStr = str.slice(7, 12);
console.log(slicedStr);
