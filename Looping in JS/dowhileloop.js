let count = 1;

do {
  console.log(count);
  count++;
} while (count <= 5);


//The do-while loop is a control flow statement that executes a block of code at least once, and then repeatedly as long as a specified condition remains true.
//It's particularly useful when you need to ensure that a block of code is executed at least once, regardless of the initial condition.