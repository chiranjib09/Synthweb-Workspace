// function parameters are the variables that you define in the function declaration to accept values when the function is called. These values are called arguments when you pass them to the function.


function multiply(a, b) {
  return a * b;
}
const result1 = multiply(5, 3);
const result2 = multiply(10, 2);

console.log(result1);
console.log(result2);
