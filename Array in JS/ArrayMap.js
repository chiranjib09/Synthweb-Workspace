//It Creates a new array populated with the results of calling a provided function on every element in the array.


const numbers = [1, 2, 3, 4, 5];
const doubled = numbers.map(num => num * 2);

console.log(doubled);
